const initState = {
  message: '',
  dayLeft: 3,
  donors: 42,
  missingValue: 167,
  isLoading: false,
};

export default function DialogMessageReducer(state = initState, action) {
  switch (action.type) {
    default: return state;
  }
}

